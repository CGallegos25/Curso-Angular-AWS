import { ConfigLayout } from '../interfaces/config-layout';

export const AMB_CONFIG: ConfigLayout = {
  layout: {
    header: {
      hidden: false,
    },
    menu: {
      hidden: false,
    }
  }
};
