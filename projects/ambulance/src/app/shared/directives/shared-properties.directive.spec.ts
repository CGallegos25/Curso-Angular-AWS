import { SharedPropertiesDirective } from './shared-properties.directive';

describe('SharedPropertiesDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedPropertiesDirective();
    expect(directive).toBeTruthy();
  });
});
